# FIX - public script + right toolbox

วางทับใน `D:\Projectsbconnect_app`

แก้:
- error Vite public module import
- เปลี่ยน devVisualEditor.js เป็น classic script
- script tag ไม่มี `type="module"` แล้ว
- tools เด้งจากขวาเป็น toolbox

หลังวางทับ:
```bat
npm run dev
```
