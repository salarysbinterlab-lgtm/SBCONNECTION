# ลำดับงานก่อนเปิดใช้จริง — ฉบับละเอียด

**SB Connect · 23 กันยายน 2569**

ทำจากบนลงล่าง **ห้ามข้าม ห้ามสลับ** ทุกขั้นเขียนไว้ 3 อย่างเสมอ
**ทำอะไร** · **ต้องเห็นอะไรถึงจะผ่าน** · **ถ้าไม่ผ่านทำยังไง**

ถ้าขั้นไหนไม่ผ่าน **หยุด** แล้วส่งผลมาให้ผม อย่าฝืนไปต่อ

```
ช่วง A  สำรองก่อนแตะอะไร            ~10 นาที   ย้อนกลับได้
ช่วง B  รัน SQL 7 ไฟล์               ~20 นาที   ★ มีขั้นที่ย้อนไม่ได้
ช่วง C  Apps Script                  ~20 นาที   ย้อนกลับได้
ช่วง D  ทดสอบของจริง 19 ข้อ          ~30 นาที   ย้อนกลับได้
ช่วง E  สำรองครั้งสุดท้าย             ~10 นาที
ช่วง F  เปิดใช้จริง                              จุดที่ไม่ย้อนแล้ว
```

---

## เตรียมก่อนเริ่ม — เปิด 4 หน้าต่างนี้ค้างไว้

| # | เปิดอะไร | ที่ไหน |
|:--:|---|---|
| 1 | Supabase SQL Editor | supabase.com → เลือกโปรเจกต์ → เมนูซ้าย **SQL Editor** |
| 2 | Apps Script | ดับเบิลคลิก `G:\My Drive\SBconnection\SBconnect_app.gscript` |
| 3 | File Explorer | `D:\Projectsbconnect_app` |
| 4 | แอป | `https://sbconnection.salary-sbinterlab.workers.dev` |

**ตรวจก่อนเลย 2 อย่าง**

- หน้า Apps Script มุมขวาบน **ต้องเป็นบัญชี Google ของบริษัทที่เป็นเจ้าของโฟลเดอร์รูป**
  ถ้าเป็นบัญชีอื่น ออกแล้วเข้าใหม่ก่อน
- เปิด `\\server-sblab\Project_IT` ใน File Explorer หนึ่งครั้ง ถ้าถามรหัสให้ใส่และติ๊กจำไว้

---

# ช่วง A — สำรองก่อนแตะอะไรทั้งนั้น

## A1 · สำรองลง server กลาง (ลองก่อนแบบไม่เขียนอะไร)

ดับเบิลคลิก `D:\Projectsbconnect_app\BACKUP_TO_SERVER.bat`
แต่ถ้าอยากลองก่อน ให้เปิด cmd ในโฟลเดอร์แล้วพิมพ์

```
BACKUP_TO_SERVER.bat /check
```

**ผ่านเมื่อ** ขึ้น `Target : \\server-sblab\Project_IT\Projectsbconnect_app`
และ `Dry run done. Nothing was written.`

**ถ้าขึ้น `STOP. The company server share was not reachable.`**
1. ยังไม่ได้ต่อเน็ตออฟฟิศ / VPN → ต่อก่อน
2. เปิด `\\server-sblab\Project_IT` ใน File Explorer หนึ่งครั้ง ใส่รหัสถ้าถูกถาม
3. ถ้าชื่อ share ไม่ตรง → สร้างไฟล์ `server_target.txt` ใน `D:\Projectsbconnect_app`
   ใส่ path จริงบรรทัดแรกบรรทัดเดียว เช่น `\\server-sblab\IT\Project_IT`

## A2 · สำรองจริงลง server

```
BACKUP_TO_SERVER.bat
```

**ผ่านเมื่อ** สรุปท้ายขึ้นครบ 4 บรรทัดนี้

```
sql          : NNN files, correct
apps_script  : N files, correct
.env         : ok
.git         : ok, code history included
```

**ถ้าขึ้น `MISSING` ที่ `sql` หรือ `apps_script`** = ตัวสำรองชุดสมบูรณ์ไม่สมบูรณ์ → **หยุด** บอกผม

## A3 · สำรองลง Google Drive

```
BACKUP_TO_GDRIVE.bat
```

**ผ่านเมื่อ** สรุปท้ายขึ้น

```
sql          : not in Drive, correct
apps_script  : not in Drive, correct
.gscript     : 0 left untouched
```

ถ้า `.gscript` ขึ้นเลขอื่นที่ไม่ใช่ 0 แปลว่ายังมีไฟล์ Apps Script อยู่ใน
`Projectsbconnect_app` — ไม่พังแล้วแต่ควรลากออกไปไว้ชั้น `SBconnection`

## A4 · สำรองฐานข้อมูล

Supabase → เมนูซ้าย **Database** → **Backups** → กดสำรอง 1 ครั้ง

**ผ่านเมื่อ** เห็นรายการ backup วันนี้ในลิสต์

## A5 · เก็บสำเนา sql ออกนอกเครื่อง

ก๊อป `D:\Projectsbconnect_app\_local_snapshots\private_<ล่าสุด>.zip` ลง USB

> **ทำไมต้องทำ** `sql/` มีสำเนาแค่ 2 ที่คือเครื่องนี้กับ server กลาง
> ไม่มีทั้งบน GitHub และ Google Drive

---

# ช่วง B — รัน SQL 7 ไฟล์

**วิธีรันทุกไฟล์เหมือนกัน**
เปิดไฟล์ด้วย Notepad → `Ctrl+A` → `Ctrl+C` → ไปที่ Supabase SQL Editor →
กด **+ New query** → `Ctrl+V` → กด **Run** (หรือ `Ctrl+Enter`)

> ⚠️ Supabase แสดงผล**เฉพาะคำสั่งสุดท้าย**ของ query เสมอ
> ไฟล์เหล่านี้เขียนมาแล้วให้สรุปผลอยู่คำสั่งสุดท้าย จึงต้อง**วางทั้งไฟล์ รันทีเดียว**
> อย่าเลือกรันทีละบรรทัด

---

## B1 · `sql/29_UI_FIELD_FIXES.sql`

**ทำอะไร** แก้บั๊ก 5 จุดที่เจอตอนตรวจ

1. หน้ากิจกรรมรวมของแอดมินพังเพราะอ้างคอลัมน์ผิดชื่อ
2. หน้าอันดับโชว์รหัสพนักงานแทนชื่อจริง
3. กิจกรรมรวมขาดข้อมูลบางช่อง
4. หน้าแรกไม่รู้ว่าวันนี้เช็คอินไปแล้ว
5. **แต้มสะสมทั้งหมดนับไม่ครบ** — แอดมินแจก 30 แต้ม แต่ยอดสะสมไม่ขึ้น

**ผ่านเมื่อ** รันจบไม่มี error สีแดง

**รันซ้ำได้ไหม** ได้ ไม่เสียหาย (เป็น `create or replace`) ถ้าไม่แน่ใจว่าเคยรันแล้ว **รันเลย**

---

## B2 · `sql/30_ROSTER_IMPORT_AND_REPORT.sql`

**ทำอะไร** โหลดรายชื่อ 336 คนจากไฟล์ HR เข้า**ตารางพัก** `roster_import`
แล้วออกรายงานเทียบกับของเดิม **ยังไม่แตะข้อมูลจริงสักแถว**

**ผ่านเมื่อ** ได้ตารางรายงานหัวข้อ ก. ถึง ซ. ตัวเลขที่ควรเห็นประมาณนี้

| หัวข้อ | ค่าที่คาด |
|---|---|
| ในไฟล์ | 336 คน |
| ในระบบตอนนี้ | 334 คน |
| ตรงกันเป๊ะ | 285 คน |
| รหัสเลขตรงแต่เขียนต่างรูปแบบ | 2 คน |
| คนใหม่ | 49 คน |
| คนที่ไม่มีในไฟล์ใหม่ | 47 คน (แต้ม 0 ทุกคน) |

**ถ้าตัวเลขต่างจากนี้มาก** → หยุด ส่งตารางมาให้ผมก่อน

**ย้อนกลับได้ไหม** ได้ 100% ไฟล์นี้อ่านอย่างเดียว

---

## B3 · `sql/32_ROSTER_FIX_IDS.sql`

**ทำอะไร** แก้ปัญหา "คนเดียวกันแต่รหัสเขียนต่างรูปแบบ" เช่นระบบเก็บ `0123`
แต่ไฟล์ HR เขียน `123` — ถ้าปล่อยผ่าน ขั้นถัดไปจะสร้างคนซ้ำ แล้วแต้มเก่าค้างอยู่รหัสเดิม

**แก้ทางไหน** ยึดรหัสใน**ฐานข้อมูล**เป็นหลัก แล้วแก้ในตารางพักให้ตรง
(ไม่ยึดไฟล์ HR เพราะ `emp_id` เป็นคีย์ที่ตารางอื่นสิบกว่าตารางอ้างถึง และไม่มี `on update cascade`)

**ผ่านเมื่อ** ตารางตรวจซ้ำท้ายไฟล์ได้ **0 แถว**

**ถ้าไม่เป็น 0** → หยุด ส่งตารางมา **ห้ามรัน `31_` เด็ดขาด**

---

## B4 · `sql/31_ROSTER_APPLY.sql` ★ ขั้นที่เปลี่ยนข้อมูลจริง

**ทำอะไร**

1. เพิ่มแผนกและตำแหน่งที่ยังไม่มี
2. อัปเดตข้อมูลพนักงานเดิม (ชื่อ สกุล ชื่อเล่น คำนำหน้า แผนก ตำแหน่ง วันเริ่มงาน)
3. เพิ่มพนักงานใหม่ 49 คน
4. คนที่ไม่มีในไฟล์ใหม่ → ตั้ง **inactive** และเพิกถอน session
5. เปิดรหัสกลางให้คนที่ยังไม่เคยตั้งรหัส

**ไม่ทำอะไรเด็ดขาด** ไม่ลบพนักงาน · ไม่แตะอีเมล · ไม่แตะแต้ม · ไม่แตะรูปโปรไฟล์ ·
ไม่แตะบทบาทแอดมิน · ไม่เปลี่ยนรหัสของคนที่ตั้งเองไปแล้ว

**ผ่านเมื่อ** ตารางยืนยันท้ายไฟล์บอก

```
พนักงาน active  = 336
อัปเดต          = 281
เพิ่มใหม่        = 55
ปิด inactive    = 16
แต้มรวมก่อน = แต้มรวมหลัง   (ต้องเท่ากัน)
```

**รันซ้ำได้ไหม** ได้ ผลเหมือนเดิมทุกครั้ง (ทดสอบแล้ว)

**ถ้า error `record ... is not assigned yet` หรือ `duplicate key`** → หยุด ส่ง error มาให้ผม

---

## B5 · `sql/26_CLEAR_TEST_DATA.sql` ★★ ย้อนกลับไม่ได้

**ทำอะไร** ล้างทุกอย่างที่เกิดจากการทดสอบ — แต้ม เช็คอิน การอ่านข่าว ภารกิจที่ส่ง
การแลกของ แจ้งเตือน ประวัติกิจกรรม แล้วรีเซ็ตแต้มทุกคนเป็น 0

**⚠️ ต้องตัดสินใจก่อนรัน** เปิดไฟล์ดูบรรทัดประมาณที่ 37–41

```sql
v_clear_points   boolean := true;   -- แต้มและกิจกรรมทั้งหมด
v_clear_news     boolean := true;   -- ข่าวทั้งหมด
v_clear_missions boolean := true;   -- ภารกิจทั้งหมด
v_clear_rewards  boolean := true;   -- ของรางวัลทั้งหมด
```

**ถ้าคุณเตรียมข่าว / ภารกิจ / ของรางวัลของจริงไว้แล้ว** ให้แก้ 3 บรรทัดล่างเป็น `false`
ก่อนรัน ไม่งั้นจะถูกลบไปด้วย

**ไม่แตะเลย** บัญชีพนักงาน · รหัสผ่าน · บัญชีแอดมิน · แผนก · ตำแหน่ง · วันหยุด ·
กฎระเบียบ · ปฏิทิน · ค่าตั้งค่าระบบ

**ผ่านเมื่อ** ตารางท้ายไฟล์บอกแต้มทุกคน = 0 และ **จำนวนพนักงานกับจำนวนรหัสผ่านเท่าเดิม**

**ถ้าจำนวนพนักงานลดลง** → หยุดทันที กู้จาก Supabase backup ที่ทำไว้ใน A4

---

## B6 · `sql/18_VERIFY_SECURITY.sql`

**ทำอะไร** ตรวจความปลอดภัยรวมทั้งระบบ อ่านอย่างเดียว ไม่แก้อะไร

**ผ่านเมื่อ** **ไม่มีบรรทัดไหนมีคำว่า `_FAIL`** เลยสักบรรทัด

**ถ้าเจอ `_FAIL`** → หยุด ส่งบรรทัดนั้นมาให้ผม (ปกติแปลว่า `29_` หรือ `17_` ยังไม่ได้รัน)

---

## B7 · `sql/34_DRIVE_ASSETS_REPORT.sql`

**ทำอะไร** รายงานว่ารูปในระบบมีกี่ใบ อยู่โฟลเดอร์ Drive ไหนบ้าง
ถ้าบัญชีใหม่เข้าถึงของเดิมไม่ได้ต้องอัปใหม่กี่ใบ **อ่านอย่างเดียว**

**ผ่านเมื่อ** ได้ตาราง ก. ถึง จ. → **ส่งตารางนี้มาให้ผม** ผมจะบอกได้ว่ารูปเก่ารอดกี่ใบ

---

# ช่วง C — Apps Script

## C1 · เปิดโปรเจกต์แล้วดู 2 อย่าง

ดับเบิลคลิก `SBconnect_app.gscript`

| ดูอะไร | ที่ไหน | บอกผมด้วย |
|---|---|---|
| โค้ดเดิมยังครบไหม | รายการ **Files** ด้านซ้าย | มีกี่ไฟล์ ชื่ออะไรบ้าง |
| Script Properties ว่างหรือมีค่า | ⚙️ **Project Settings** → เลื่อนล่างสุด | ว่าง หรือมีกี่ตัว |

**นี่คือข้อมูลที่ผมรอ** สองอย่างนี้ตัดสินว่าเหลืองานอีกเท่าไหร่

## C2 · เอาไฟล์ใหม่เข้าไป

กด **+** ข้างคำว่า Files เพื่อสร้าง หรือเปิดไฟล์เดิมแล้ววางทับ
(ตอนตั้งชื่อ **ไม่ต้องใส่ `.gs`**)

| ชื่อไฟล์ใน Apps Script | ทำยังไง | ก๊อปจาก |
|---|---|---|
| `SBConnect_Setup_Folders` | **สร้างใหม่** | `D:\Projectsbconnect_app\apps_script\SBConnect_Setup_Folders.gs` |
| `SBConnect_Drive_Upload_API` | **วางทับทั้งไฟล์** | `...\SBConnect_Drive_Upload_API.gs` |
| `SBConnect_Image_Sharing_Audit` | **วางทับทั้งไฟล์** | `...\SBConnect_Image_Sharing_Audit.gs` |
| `SBConnect_IT_Request_Register` | มีแล้วปล่อยไว้ · ไม่มีให้สร้าง | `...\SBConnect_IT_Request_Register.gs` |

กด 💾 **Save** ทุกไฟล์

> ไฟล์ที่ 4 ต้องมี **แม้โมดูล IT Request จะยัง hold อยู่** เพราะ `doPost` เรียก
> `itReqFlushRegister_()` ถ้าไม่มีจะพังตอนมีคำขอนั้นวิ่งเข้ามา

## C3 · ใส่ค่าความลับ 2 ตัว (ถ้าหายไป)

⚙️ **Project Settings** → ล่างสุด **Script Properties** → **Add script property**

| Property | ค่า | หาจากไหน |
|---|---|---|
| `SUPABASE_URL` | `https://tmcbblwfucwauksenqqr.supabase.co` | มีใน `.env` แล้ว |
| `SUPABASE_SERVICE_ROLE_KEY` | คีย์ยาวๆ | Supabase → Settings → API → `service_role` **secret** |

> ⚠️ `service_role` key อยู่ได้ที่เดียวคือตรงนี้
> **ห้ามใส่ใน `.env` ห้ามใส่ในโค้ดหน้าเว็บ ห้ามส่งมาในแชท**

**ยังไม่ต้องใส่ `FOLDER_*` สักตัว** — C5 จะใส่ให้เอง

## C4 · `SB_CHECK_LEGACY` — ตรวจอย่างเดียว

เลือกฟังก์ชัน **`SB_CHECK_LEGACY`** จาก dropdown ด้านบน → **Run**

ครั้งแรก Google จะขอสิทธิ์ →
**Review permissions** → เลือกบัญชี → **Advanced** → **Go to (ชื่อโปรเจกต์)** → **Allow**

ดูผลที่ **Execution log** ด้านล่าง → **ส่งข้อความในนั้นมาให้ผมทั้งหมด**

| ถ้าเห็น | แปลว่า |
|---|---|
| `OK` หมดทุกบรรทัด | ของเดิมอยู่ครบ รูปเก่าไม่หาย |
| `พัง` หมดทุกบรรทัด | บัญชีนี้เข้าไม่ถึงของเดิม ต้องเริ่มชุดใหม่ + อัปรูปใหม่ |
| ปนกัน | อันที่ OK จะถูกใช้ต่อ อันที่พังจะสร้างใหม่ทีละอัน |

## C5 · `SB_SETUP_ALL` — ติดตั้งจริง

เลือก **`SB_SETUP_ALL`** → **Run**

จะสร้างผังนี้ให้ (ของไหนมีอยู่แล้วจะใช้ของเดิม ไม่สร้างซ้ำ)

```
SB_CONNECT_APP/
├── 01_profile            รูปโปรไฟล์
├── 02_news               รูปข่าว
├── 03_missions           รูปภารกิจ
├── 04_rewards            รูปของรางวัล
├── 05_mission_evidence   หลักฐานภารกิจ
├── 06_attachments        ไฟล์แนบ
├── 07_games/             ★ โฟลเดอร์แม่ของเกม
│   └── wheel             ★ รูปกงล้อรางวัล
└── 08_quotation/ pdf images
```

**ผ่านเมื่อ** ปิดท้ายด้วย `สรุป: ใช้ของเดิม N รายการ · สร้างใหม่ M รายการ`
และ **ไม่มีบรรทัดขึ้นต้นด้วย `ขาด`**

ถ้ามี `ขาด SUPABASE_...` แปลว่า C3 ยังไม่ครบ → กลับไปใส่แล้วรันใหม่

**รันซ้ำได้ไหม** ได้ ไม่สร้างซ้ำ ผลเหมือนเดิมทุกครั้ง

## C6 · `SB_SHOW_CONFIG` — เอา ID ใหม่บอก Supabase

เลือก **`SB_SHOW_CONFIG`** → **Run** → ใน log จะมีบล็อก

```
── SQL: ก๊อปไปวางใน Supabase > SQL Editor แล้วกด Run ──
insert into public.app_settings(key, value, description, is_public) values
  ...
on conflict (key) do update set ...
```

ก๊อปตั้งแต่ `insert` ถึง `;` → ไปวางใน **Supabase SQL Editor** → **Run**

**ผ่านเมื่อ** รันจบไม่มี error

## C7 · Deploy

**ถ้ามี deployment เดิมอยู่ — ใช้ทางนี้ URL จะไม่เปลี่ยน**

1. **Deploy** (มุมขวาบน) → **Manage deployments**
2. กด ✏️ ดินสอ ที่ deployment เดิม
3. ช่อง **Version** → เลือก **New version**
4. **Deploy**

→ URL `/exec` เดิมใช้ได้ทันที **ข้าม C8 ไปเลย**

**ถ้าไม่มี deployment เดิม**

1. **Deploy** → **New deployment** → เฟือง ⚙️ → **Web app**
2. Execute as: **Me**
3. Who has access: **Anyone**
4. **Deploy** → ก๊อป URL ที่ลงท้าย `/exec`

## C8 · *(เฉพาะกรณีได้ URL ใหม่)* แก้ endpoint แล้ว build ใหม่

ค่านี้ถูกฝังตอน **build** ไม่ใช่ตอนเปิดเว็บ **แก้ที่เดียวไม่พอ ต้องแก้ทั้ง 2 ที่**

1. เปิด `D:\Projectsbconnect_app\.env` ด้วย Notepad แก้บรรทัด
   ```
   VITE_DRIVE_UPLOAD_ENDPOINT=https://script.google.com/macros/s/<ของใหม่>/exec
   ```
2. GitHub → repo `SBCONNECTION` → **Settings** → **Secrets and variables** →
   **Actions** → แท็บ **Variables** → แก้ `VITE_DRIVE_UPLOAD_ENDPOINT`
3. รัน `push.bat` แล้วรอ Actions ขึ้นเขียว

## C9 · `IMAGE_SHARING_CHECK` — ตรวจสิทธิ์รูป

เลือก **`IMAGE_SHARING_CHECK`** → **Run**

**ผ่านเมื่อ** ได้ `ยังไม่เปิดให้ดู 0`

**ถ้าไม่ใช่ 0** → รัน **`IMAGE_SHARING_FIX`** → แล้วรัน `IMAGE_SHARING_CHECK` ซ้ำให้ได้ 0

## C10 · ตรวจปลายทางด้วยตาตัวเอง

เปิด URL `/exec` ในเบราว์เซอร์ตรงๆ จะได้ JSON **ต้องมี 2 บรรทัดนี้**

```
"gameFolderConfigured": true
"gameBuckets": [ ... "game_wheel" ... ]
```

ถ้าไม่มี = C5 ยังไม่สำเร็จ หรือ C7 ยังไม่ได้ deploy เวอร์ชันใหม่

---

# ช่วง D — ทดสอบของจริง 19 ข้อ

เปิด 2 หน้าต่าง: หน้าต่างปกติ = แอดมิน · หน้าต่าง **incognito** = พนักงาน

## ฝั่งแอดมิน (ข้อ 1–6)

| # | ทำ | ต้องได้ |
|:--:|---|---|
| 1 | เข้าระบบด้วยบัญชีแอดมิน | เข้าได้ เมนูแอดมินครบ |
| 2 | **เพิ่มข่าว 1 อัน พร้อมแนบรูป** ตั้งแต้ม 5 | รูปขึ้นจริง ไม่ใช่รูปแตก |
| 3 | **เพิ่มภารกิจ 1 อัน พร้อมรูป** ตั้งแต้ม 20 เลือกแบบ **ต้องอนุมัติ** | รูปขึ้น |
| 4 | **เพิ่มของรางวัล 1 อัน พร้อมรูป** ตั้งแต้ม 10 สต็อก 2 | รูปขึ้น |
| 5 | เปิดหน้ารายชื่อพนักงาน | **336 คน** สถานะ active |
| 6 | เปิดหน้าอันดับฝั่งแอดมิน | แต้มทุกคนเป็น 0 |

## ฝั่งพนักงาน (ข้อ 7–15) — หน้าต่าง incognito

| # | ทำ | ต้องได้ |
|:--:|---|---|
| 7 | พนักงานใหม่เข้าครั้งแรก ด้วยรหัสกลาง | เข้าได้ แล้วถูกบังคับตั้งรหัสใหม่ |
| 8 | กดเช็คอิน · แล้วกดซ้ำอีกที | ครั้งแรกได้แต้ม · ครั้งที่สองขึ้นว่าเช็คอินแล้ว ไม่ได้แต้มซ้ำ |
| 9 | อ่านข่าวจากข้อ 2 | เห็นข่าว · รูปขึ้น · ได้ 5 แต้ม |
| 10 | ส่งภารกิจจากข้อ 3 | **ยังไม่ได้แต้ม** สถานะรออนุมัติ |
| 11 | **แก้รูปโปรไฟล์ตัวเอง** | รูปขึ้น ← ข้อที่พิสูจน์ว่าอัปโหลดใช้ได้จริง |
| 12 | แลกของรางวัลจากข้อ 4 | แต้มถูกหัก 10 · สต็อกเหลือ 1 |
| 13 | หน้าอันดับ → กด **ดูทั้งหมด** | เห็นครบทุกคน · ค้นหาได้ · กรองแผนกได้ · ปุ่มไปที่อันดับฉันใช้ได้ |
| 14 | เปิดเมนู **Social** | dropdown เปิด · 3 กลุ่ม (YouTube / Marketplace / Community) · กดแล้วเปิดแท็บใหม่ถูกช่อง |
| 15 | ชี้เมาส์ที่ปุ่มและเมนูต่างๆ | มีสีพาสเทลจางขึ้นมา สลับสีไปเรื่อย |

## วนกลับฝั่งแอดมิน (ข้อ 16–19)

| # | ทำ | ต้องได้ |
|:--:|---|---|
| 16 | เปิดหน้าภารกิจที่ส่งเข้ามา | เห็นงานจากข้อ 10 สถานะ Pending |
| 17 | กดอนุมัติ | ฝั่งพนักงานได้ 20 แต้ม อันดับขยับ |
| 18 | เปิดหน้ารายการแลกของ | เห็นรายการจากข้อ 12 |
| 19 | เปิดสมุดแต้ม / กิจกรรมรวม | เห็นครบทุกหมวด: แต้ม เช็คอิน ข่าว ภารกิจ แลกของ เข้าระบบ |

> **ข้อ 2 · 3 · 4 · 11 คือหัวใจ** สี่ข้อนี้ผ่าน = Apps Script + Drive +
> Script Properties กลับมาทำงานครบวงจรแล้ว
>
> **ถ้าได้รูปแตก** กด `F12` → แท็บ **Console** → ถ่ายรูปข้อความ error ส่งมาให้ผม
> และแท็บ **Network** ดูคำขอไป `script.google.com` ว่าตอบอะไรกลับมา

---

# ช่วง E — สำรองครั้งสุดท้าย

```
D:\Projectsbconnect_app\push.bat
```

ตัวเดียวทำครบ 7 ขั้น ใส่ commit message เช่น

```
go-live: roster 336, social menu, hover tint, game image folders, 3-way backup
```

**ผ่านเมื่อเห็นครบทุกบรรทัด**

```
[2/7]  OK    .env is ignored
[3/7]  OK    all checks passed
[5/7]  push สำเร็จ
[6/7]  OK    code copied to Google Drive
[7/7]  OK    complete copy on the server
```

แล้วไป GitHub → **Actions** → **Deploy Cloudflare** ต้องขึ้น ✅ เขียว

**สุดท้าย** รอไอคอน Google Drive มุมขวาล่างขึ้นว่าอัปโหลดเสร็จ **ก่อนปิดเครื่อง**
ไม่งั้นไฟล์ยังอยู่แค่ในเครื่อง ไม่ได้ขึ้นคลาวด์

**ถ้า `[3/7] npm run check` ไม่ผ่าน** → push จะหยุดเอง ไม่มีอะไรขึ้น GitHub
ส่ง error มาให้ผม

---

# ช่วง F — เปิดใช้จริง

1. รีเฟรชแอปหนึ่งรอบ ตรวจว่าเป็นเวอร์ชันใหม่
2. แจ้งพนักงาน: เข้าที่ `https://sbconnection.salary-sbinterlab.workers.dev`
   ใส่**รหัสพนักงาน** + **รหัสกลาง** แล้วระบบจะให้ตั้งรหัสใหม่ของตัวเอง
3. บอกให้กด **ติดตั้ง / Add to Home Screen** เพื่อใช้แบบแอป
4. เตรียมรับเคส "เข้าไม่ได้" — แอดมินรีเซ็ตรหัสให้ได้จากหน้าแอดมิน

---

# ตารางสรุป: ขั้นไหนย้อนได้ ขั้นไหนไม่ได้

| ขั้น | ย้อนกลับ | วิธีย้อน |
|---|:--:|---|
| A ทั้งหมด | ✅ | ไม่ได้เปลี่ยนอะไร |
| B1 `29_` | ✅ | รันซ้ำได้ ไม่เสียหาย |
| B2 `30_` | ✅ | อ่านอย่างเดียว |
| B3 `32_` | ✅ | แตะแค่ตารางพัก |
| B4 `31_` | ⚠️ | รันซ้ำได้ ถ้าจะย้อนจริงต้องกู้จาก Supabase backup (A4) |
| **B5 `26_`** | ❌ | **ย้อนไม่ได้** ต้องกู้จาก Supabase backup (A4) เท่านั้น |
| B6 `18_` B7 `34_` | ✅ | อ่านอย่างเดียว |
| C ทั้งหมด | ✅ | Apps Script เก็บเวอร์ชันเก่าไว้ · deployment ย้อนเวอร์ชันได้ |
| D ทั้งหมด | ✅ | เป็นการทดสอบ ลบทิ้งทีหลังได้ |
| E `push.bat` | ⚠️ | git revert ได้ แต่ backup ที่เขียนไปแล้วไม่ย้อน |

---

# สิ่งที่ยังไม่พร้อม — อย่าเพิ่งเปิดให้พนักงานใช้

| ระบบ | สถานะจริง |
|---|---|
| **เกมกงล้อ** | มีแค่ไฟล์หน้าตา `src/games/wheel/WheelGame.tsx` · **ยังไม่ถูก import ที่ไหนเลย** · ไม่มีตาราง ไม่มี RPC `spin_wheel` ไม่มีหน้าแอดมิน → พนักงานจะมองไม่เห็น ไม่ต้องทำอะไร ปล่อยไว้ได้ |
| **IT Request** | `sql/27_` `28_` เตรียมไว้แต่ยังไม่ได้รัน → ไม่กระทบระบบปัจจุบัน |
| **Quotation** | hold ตามที่คุยกัน |
| **แต้มทศนิยม** | `sql/33_` เป็นสคริปต์ตรวจ ยังไม่ได้แปลงจริง · **ถ้าจะทำ ต้องทำก่อนเขียนระบบเกม** ไม่งั้นฟังก์ชันเกมต้องรื้อซ้ำ เพราะฟังก์ชันแต้มเดิมประกาศตัวแปรเป็น `integer` ค่า 0.5 จะถูกปัดเศษเงียบๆ |

---

# สิ่งที่ผมยืนยันแทนคุณไม่ได้

1. **เข้า Supabase production ไม่ได้** ตัวเลขทุกตัวในช่วง B ต้องมาจากการรันจริง
2. **เข้า Google Drive และ Apps Script ของคุณไม่ได้** สถานะ Script Properties
   ต้องมาจาก `SB_CHECK_LEGACY` เท่านั้น
3. **เข้า `\\server-sblab\Project_IT` ไม่ได้** ยืนยันไม่ได้ว่าเขียนได้จริงจนกว่าจะรัน A2
4. **รัน `npm run dev` / `npm run check` จากที่นี่ไม่ได้** (npm ถูกบล็อก)
5. **ข้อ 2 · 3 · 4 · 11 ในช่วง D ทดสอบแทนไม่ได้** ต้องอัปโหลดรูปจริงจากเครื่องคุณ

**สิ่งที่ตรวจไปแล้วจริง** โค้ด Apps Script ตรวจ syntax ผ่าน + จำลองการทำงาน 28 เคสผ่าน ·
ตัวติดตั้งโฟลเดอร์ 20 เคสผ่าน · `34_DRIVE_ASSETS_REPORT.sql` รันจริงบน PostgreSQL 16
ได้ผลถูกต้อง · ไฟล์ `.bat` ทั้ง 3 ตัว ASCII ล้วน CRLF วงเล็บสมดุล label ครบ ไม่มี `/MIR`

---

# สิ่งที่ผมรอจากคุณ 3 อย่าง

1. **log ของ `SB_CHECK_LEGACY`** (ช่วง C4)
2. **ตารางผลของ `sql/34_DRIVE_ASSETS_REPORT.sql`** (ช่วง B7)
3. **รายการไฟล์ใน Apps Script + จำนวน Script Properties** (ช่วง C1)

สามอย่างนี้บอกได้ทันทีว่ารูปเก่ารอดกี่ใบ และเหลืองานอีกเท่าไหร่
