# ลำดับงานก่อนเปิดใช้จริง — ฉบับละเอียด (รอบที่ 2)

**SB Connect · 23 กันยายน 2569 · รวมการแก้บั๊กจากการทดสอบบน iPhone แล้ว**

ทำจากบนลงล่าง **ห้ามข้าม ห้ามสลับ** ทุกขั้นเขียนไว้ 3 อย่างเสมอ
**ทำอะไร** · **ต้องเห็นอะไรถึงจะผ่าน** · **ถ้าไม่ผ่านทำยังไง**

ถ้าขั้นไหนไม่ผ่าน **หยุด** แล้วส่งผลมาให้ผม อย่าฝืนไปต่อ

```
ช่วง A  สำรองก่อนแตะอะไร            ~10 นาที   ย้อนกลับได้
ช่วง B  รัน SQL 8 ไฟล์               ~25 นาที   ★ มีขั้นที่ย้อนไม่ได้
ช่วง C  Apps Script                  ~20 นาที   ย้อนกลับได้
ช่วง D  build โค้ดหน้าเว็บที่แก้ใหม่   ~10 นาที   ย้อนกลับได้
ช่วง E  ทดสอบของจริง 23 ข้อ          ~35 นาที   ย้อนกลับได้
ช่วง F  สำรองครั้งสุดท้าย + เปิดใช้     ~10 นาที   จุดที่ไม่ย้อนแล้ว
```

---

## รอบนี้แก้อะไรไปบ้าง

| # | อาการที่เจอ | สาเหตุจริง | แก้ที่ไหน |
|:--:|---|---|---|
| 1 | บัตรพนักงานพลิกแล้ว **ตัวหนังสือกลับด้าน** บน iPhone | React ไม่เติม vendor prefix ให้ Safari จึงไม่รู้จัก `backface-visibility` ผลคือวาดสองหน้าซ้อนกัน หน้าหลังที่หมุน 180° เลยทับแบบกลับด้าน | `UserDashboard.tsx` |
| 2 | ทุกคนขึ้นว่า **"เจ้าหน้าที่ / Staff"** | โค้ดตัดสินจาก `role` อย่างเดียว และฐานข้อมูลไม่เคยส่ง `position` / `department` มาให้หน้าแรก | `UserDashboard.tsx` + `sql/35_` |
| 3 | **รูปโปรไฟล์หาย** หลัง logout แล้ว login ใหม่ | `get_home_dashboard` ไม่ส่ง `avatar_url` กลับมา และโค้ดยัง `removeItem` แคชรูปทิ้งหลังอัปโหลดสำเร็จ | `UserDashboard.tsx` + `sql/35_` |
| 4 | **กดอ่านโน้ตในปฏิทินไม่ได้** | ช่องวันเป็น `<div>` เฉย ๆ มีแค่ `title` ซึ่งบนมือถือไม่มีทางแสดง | `UserDashboard.tsx` |
| 5 | อยากให้ **ประกาศเด้งหน้าแรก** | ยังไม่มีของเดิม | `UserDashboard.tsx` |
| 6 | **รหัสพนักงานโผล่ในหน้าอันดับ** | ซ่อนที่หน้าเว็บอย่างเดียวไม่พอ เปิด DevTools ก็ยังอ่านรหัสทุกคนได้ | `UserDashboard.tsx` + `sql/35_` |

> **ข้อ 6 สำคัญ** การซ่อนจริงต้องตัดตั้งแต่ฐานข้อมูล `sql/35_` ทำให้หน้าอันดับส่งกลับ
> `emp_id` เฉพาะแถวของตัวผู้เรียกเอง แถวคนอื่นเป็น `null` และเพิ่มธง `is_me` มาแทน
> เพื่อให้ยังไฮไลต์แถวตัวเองได้โดยไม่ต้องรู้รหัสใคร **ฝั่งแอดมินใช้ฟังก์ชัน `admin_*`
> คนละตัว จึงยังเห็นครบทุกอย่างเหมือนเดิม**

---

## เตรียมก่อนเริ่ม — เปิด 4 หน้าต่างนี้ค้างไว้

| # | เปิดอะไร | ที่ไหน |
|:--:|---|---|
| 1 | Supabase SQL Editor | supabase.com → เลือกโปรเจกต์ → เมนูซ้าย **SQL Editor** |
| 2 | Apps Script | ดับเบิลคลิก `G:\My Drive\SBconnection\SBconnect_app.gscript` |
| 3 | File Explorer | `D:\Projectsbconnect_app` |
| 4 | แอป | `https://sbconnection.salary-sbinterlab.workers.dev` |

**ตรวจก่อนเลย 2 อย่าง**

- หน้า Apps Script มุมขวาบน **ต้องเป็นบัญชี Google ของบริษัทที่เป็นเจ้าของโฟลเดอร์รูป**
- เปิด `\\server-sblab\Project_IT` ใน File Explorer หนึ่งครั้ง ถ้าถามรหัสให้ใส่และติ๊กจำไว้

---

# ช่วง A — สำรองก่อนแตะอะไรทั้งนั้น

## A1 · ลองก่อนแบบไม่เขียนอะไร

```
D:\Projectsbconnect_app\BACKUP_TO_SERVER.bat /check
```

**ผ่านเมื่อ** ขึ้น `Target : \\server-sblab\Project_IT\Projectsbconnect_app`
และ `Dry run done. Nothing was written.`

**ถ้าขึ้น `STOP. The company server share was not reachable.`**
1. ต่อเน็ตออฟฟิศ / VPN ก่อน
2. เปิด `\\server-sblab\Project_IT` ใน File Explorer หนึ่งครั้ง ใส่รหัสถ้าถูกถาม
3. ถ้าชื่อ share ไม่ตรง → สร้าง `server_target.txt` ใน `D:\Projectsbconnect_app`
   ใส่ path จริงบรรทัดแรกบรรทัดเดียว

## A2 · สำรองจริงลง server (ชุดสมบูรณ์)

```
D:\Projectsbconnect_app\BACKUP_TO_SERVER.bat
```

**ผ่านเมื่อ** สรุปท้ายขึ้นครบ

```
sql          : NNN files, correct
apps_script  : N files, correct
.env         : ok
.git         : ok, code history included
```

## A3 · สำรองลง Google Drive

```
D:\Projectsbconnect_app\BACKUP_TO_GDRIVE.bat
```

**ผ่านเมื่อ**

```
sql          : not in Drive, correct
apps_script  : not in Drive, correct
.gscript     : 0 left untouched
```

## A4 · สำรองฐานข้อมูล

Supabase → **Database** → **Backups** → กดสำรอง 1 ครั้ง
**ผ่านเมื่อ** เห็นรายการ backup ของวันนี้

## A5 · ก๊อป zip ออกนอกเครื่อง

ก๊อป `D:\Projectsbconnect_app\_local_snapshots\private_<ล่าสุด>.zip` ลง USB
`sql/` มีสำเนาแค่ 2 ที่คือเครื่องนี้กับ server กลาง ไม่มีบน GitHub และ Drive

---

# ช่วง B — รัน SQL 8 ไฟล์

**วิธีรันทุกไฟล์เหมือนกัน**
เปิดไฟล์ด้วย Notepad → `Ctrl+A` → `Ctrl+C` → Supabase SQL Editor →
**+ New query** → `Ctrl+V` → **Run** (`Ctrl+Enter`)

> ⚠️ Supabase แสดงผล**เฉพาะคำสั่งสุดท้าย** ไฟล์เหล่านี้เขียนมาให้สรุปอยู่คำสั่งสุดท้าย
> จึงต้อง **วางทั้งไฟล์ รันทีเดียว** อย่าเลือกรันทีละบรรทัด

| ลำดับ | ไฟล์ | ทำอะไร | ผ่านเมื่อ |
|:--:|---|---|---|
| B1 | `29_UI_FIELD_FIXES.sql` | แก้บั๊ก 5 จุดเดิม | ไม่มี error · รันซ้ำได้ ไม่แน่ใจก็รันเลย |
| B2 | `30_ROSTER_IMPORT_AND_REPORT.sql` | โหลด 336 คนเข้าตารางพัก + รายงาน | ได้ตาราง ก. ถึง ซ. |
| B3 | `32_ROSTER_FIX_IDS.sql` | แก้รหัสที่เขียนต่างรูปแบบ | ตารางตรวจซ้ำได้ **0 แถว** |
| B4 | `31_ROSTER_APPLY.sql` ★ | อัปเดตรายชื่อจริง | active = **336** · แต้มรวมก่อน = หลัง |
| B5 | `26_CLEAR_TEST_DATA.sql` ★★ | ล้างแต้มและกิจกรรมทดสอบ | แต้มทุกคน = 0 · จำนวนพนักงานเท่าเดิม |
| B6 | **`35_PRIVACY_PROFILE_FIXES.sql`** ← **ใหม่รอบนี้** | ซ่อนรหัสพนักงาน · ส่ง avatar/ตำแหน่ง/แผนกให้หน้าแรก | ตาราง 4 บรรทัด **ต้อง `ok` ทุกบรรทัด ห้ามมี `FAIL`** |
| B7 | `18_VERIFY_SECURITY.sql` | ตรวจความปลอดภัยรวม | **ห้ามมีบรรทัดไหนมีคำว่า `_FAIL`** |
| B8 | `34_DRIVE_ASSETS_REPORT.sql` | รายงานรูปในระบบ (อ่านอย่างเดียว) | ได้ตาราง ก. ถึง จ. → **ส่งมาให้ผม** |

## รายละเอียดขั้นที่ต้องระวัง

### B3 — ถ้าไม่ได้ 0 แถว
**ห้ามรัน B4 เด็ดขาด** ไม่งั้นจะได้พนักงานซ้ำ 2 คน แล้วแต้มเก่าค้างอยู่รหัสเดิม
ส่งตารางมาให้ผมก่อน

### B5 — ย้อนกลับไม่ได้ ★★
**ก่อนรัน เปิดไฟล์ดูบรรทัดประมาณ 37–41**

```sql
v_clear_points   boolean := true;   -- แต้มและกิจกรรมทั้งหมด
v_clear_news     boolean := true;   -- ข่าวทั้งหมด
v_clear_missions boolean := true;   -- ภารกิจทั้งหมด
v_clear_rewards  boolean := true;   -- ของรางวัลทั้งหมด
```

**ถ้าเตรียมข่าว / ภารกิจ / ของรางวัลของจริงไว้แล้ว** แก้ 3 บรรทัดล่างเป็น `false`
ไม่งั้นโดนลบไปด้วย

**ไม่แตะเลย** บัญชีพนักงาน · รหัสผ่าน · บัญชีแอดมิน · แผนก · ตำแหน่ง · วันหยุด · ปฏิทิน · ค่าตั้งค่าระบบ

**ถ้าจำนวนพนักงานลดลง** หยุดทันที กู้จาก Supabase backup ที่ทำไว้ใน A4

### B6 — ไฟล์ใหม่ของรอบนี้
ต้องรัน **หลัง** `29_` เสมอ เพราะสร้างทับฟังก์ชันสองตัวที่ `29_` วางรากไว้
ทดสอบมาแล้วบน PostgreSQL 16 จริง 6 เคส: ซ่อนรหัสคนอื่น · เห็นรหัสตัวเอง ·
`is_me` ถูกต้องทั้งสองบัญชี · `avatar_url`/`position`/`department` มาครบ ·
อันดับบนหน้าแรกก็ซ่อนรหัสด้วย · session หมดอายุไม่พัง

---

# ช่วง C — Apps Script

## C1 · เปิดโปรเจกต์ แล้วบอกผม 2 อย่าง

| ดูอะไร | ที่ไหน |
|---|---|
| โค้ดเดิมยังครบไหม | รายการ **Files** ด้านซ้าย มีกี่ไฟล์ ชื่ออะไร |
| Script Properties ว่างหรือมีค่า | ⚙️ **Project Settings** → ล่างสุด |

## C2 · เอาไฟล์ใหม่เข้าไป

| ชื่อไฟล์ใน Apps Script | ทำยังไง |
|---|---|
| `SBConnect_Setup_Folders` | **สร้างใหม่** ก๊อปจาก `apps_script\SBConnect_Setup_Folders.gs` |
| `SBConnect_Drive_Upload_API` | **วางทับทั้งไฟล์** |
| `SBConnect_Image_Sharing_Audit` | **วางทับทั้งไฟล์** |
| `SBConnect_IT_Request_Register` | มีแล้วปล่อยไว้ · ไม่มีให้สร้าง |

กด 💾 Save ทุกไฟล์ (ตอนตั้งชื่อ **ไม่ต้องใส่ `.gs`**)

## C3 · ใส่ค่าความลับ 2 ตัว (ถ้าหายไป)

⚙️ Project Settings → **Script Properties** → **Add script property**

| Property | ค่า |
|---|---|
| `SUPABASE_URL` | `https://tmcbblwfucwauksenqqr.supabase.co` |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase → Settings → API → `service_role` **secret** |

> ⚠️ `service_role` key อยู่ได้ที่เดียวคือตรงนี้ **ห้ามใส่ใน `.env` ห้ามส่งมาในแชท**
> ยังไม่ต้องใส่ `FOLDER_*` สักตัว C5 จะใส่ให้เอง

## C4 · `SB_CHECK_LEGACY` → **ส่ง log มาให้ผม**

เลือกฟังก์ชันจาก dropdown → **Run**
ครั้งแรก Google ขอสิทธิ์ → Review permissions → เลือกบัญชี → Advanced → Go to → Allow

| ถ้าเห็น | แปลว่า |
|---|---|
| `OK` หมด | ของเดิมอยู่ครบ รูปเก่าไม่หาย |
| `พัง` หมด | บัญชีนี้เข้าไม่ถึงของเดิม ต้องอัปรูปใหม่ |
| ปนกัน | อันที่ OK ใช้ต่อ อันที่พังสร้างใหม่ทีละอัน |

## C5 · `SB_SETUP_ALL`

**ผ่านเมื่อ** ปิดท้ายด้วย `สรุป: ใช้ของเดิม N รายการ · สร้างใหม่ M รายการ`
และ **ไม่มีบรรทัดขึ้นต้นด้วย `ขาด`**

## C6 · `SB_SHOW_CONFIG` → ก๊อป SQL ไปรันใน Supabase

ในผลจะมีบล็อก `insert into public.app_settings...` ก๊อปตั้งแต่ `insert` ถึง `;`
ไปวางใน **Supabase SQL Editor** → Run

## C7 · Deploy

**ถ้ามี deployment เดิม** → Deploy → **Manage deployments** → ✏️ →
Version: **New version** → Deploy → **URL เดิมไม่เปลี่ยน ข้าม C8**

**ถ้าไม่มี** → Deploy → New deployment → ⚙️ → Web app →
Execute as **Me** · Who has access **Anyone** → Deploy → ก๊อป URL `/exec`

## C8 · *(เฉพาะกรณีได้ URL ใหม่)*

ค่านี้ถูกฝังตอน **build** ต้องแก้ **2 ที่**
1. `D:\Projectsbconnect_app\.env` → `VITE_DRIVE_UPLOAD_ENDPOINT=...`
2. GitHub → Settings → Secrets and variables → Actions → **Variables**

## C9 · `IMAGE_SHARING_CHECK`

**ผ่านเมื่อ** ได้ `ยังไม่เปิดให้ดู 0`
ถ้าไม่ใช่ 0 → รัน `IMAGE_SHARING_FIX` → ตรวจซ้ำให้ได้ 0

## C10 · เปิด URL `/exec` ในเบราว์เซอร์

**ต้องมี 2 บรรทัดนี้ใน JSON**

```
"gameFolderConfigured": true
"gameBuckets": [ ... "game_wheel" ... ]
```

---

# ช่วง D — build โค้ดหน้าเว็บที่แก้ใหม่

**ข้ามขั้นนี้ไม่ได้** การแก้ 6 อย่างในรอบนี้อยู่ใน `UserDashboard.tsx`
ซึ่งต้อง build ใหม่ถึงจะมีผล ต่างจาก SQL ที่เห็นผลทันที

```
D:\Projectsbconnect_app\push.bat
```

ใส่ commit message เช่น

```
fix: ios card flip, real position on card, avatar persistence, calendar notes, hide emp_id in ranking
```

**ผ่านเมื่อ**

```
[2/7]  OK    .env is ignored
[3/7]  OK    all checks passed
[5/7]  push สำเร็จ
[6/7]  OK    code copied to Google Drive
[7/7]  OK    complete copy on the server
```

แล้วไป GitHub → **Actions** → **Deploy Cloudflare** ต้องขึ้น ✅ เขียว

**ถ้า `[3/7] npm run check` ไม่ผ่าน** push จะหยุดเอง ไม่มีอะไรขึ้น GitHub
ส่ง error มาให้ผม

**หลัง Actions เขียวแล้ว** เปิดแอปบนมือถือ **ปิดแอปแล้วเปิดใหม่ 1 รอบ**
(PWA แคช service worker ไว้ ถ้าไม่ปิดเปิดจะยังเห็นของเก่า)

---

# ช่วง E — ทดสอบของจริง 23 ข้อ

เปิด 2 หน้าต่าง: ปกติ = แอดมิน · **incognito** = พนักงาน
ข้อที่มี 📱 ให้ทดสอบบน **iPhone จริง** ด้วย

## ฝั่งแอดมิน (1–7)

| # | ทำ | ต้องได้ |
|:--:|---|---|
| 1 | เข้าระบบด้วยบัญชีแอดมิน | เข้าได้ เมนูครบ |
| 2 | **เพิ่มข่าว 1 อัน พร้อมแนบรูป** ตั้งแต้ม 5 | รูปขึ้นจริง ไม่ใช่รูปแตก |
| 3 | **เพิ่มภารกิจ 1 อัน พร้อมรูป** ตั้งแต้ม 20 แบบ **ต้องอนุมัติ** | รูปขึ้น |
| 4 | **เพิ่มของรางวัล 1 อัน พร้อมรูป** แต้ม 10 สต็อก 2 | รูปขึ้น |
| 5 | **ปฏิทิน → คลิกวันพรุ่งนี้ → ใส่บันทึก** เช่น "ประชุมใหญ่ 9 โมง" | บันทึกสำเร็จ |
| 6 | หน้ารายชื่อพนักงาน | **336 คน** active · **เห็นรหัสพนักงานครบทุกคน** |
| 7 | หน้าอันดับฝั่งแอดมิน | แต้มทุกคน 0 · **ยังเห็นรหัสพนักงาน** |

## ฝั่งพนักงาน (8–19)

| # | ทำ | ต้องได้ |
|:--:|---|---|
| 8 | พนักงานใหม่เข้าครั้งแรกด้วยรหัสกลาง | เข้าได้ แล้วบังคับตั้งรหัสใหม่ |
| 9 | ดูบัตรพนักงานหน้าแรก | **ขึ้นตำแหน่งจริงกับแผนกจริง** ไม่ใช่คำว่า Staff |
| 10 | 📱 **แตะบัตรให้พลิก** บน iPhone | ด้านหลังขึ้นเงื่อนไขการใช้บัตร **ตัวหนังสืออ่านออก ไม่กลับด้าน** |
| 11 | 📱 แตะพลิกกลับ | ด้านหน้ากลับมาปกติ ไม่มีเงาซ้อน |
| 12 | **แก้รูปโปรไฟล์ตัวเอง** | รูปขึ้นทันที |
| 13 | **logout แล้ว login ใหม่** | **รูปโปรไฟล์ยังอยู่** ← ข้อที่แก้รอบนี้ |
| 14 | กดเช็คอิน แล้วกดซ้ำ | ครั้งแรกได้แต้ม · ครั้งสองขึ้นว่าเช็คอินแล้ว |
| 15 | อ่านข่าวจากข้อ 2 | เห็นข่าว · รูปขึ้น · ได้ 5 แต้ม |
| 16 | ส่งภารกิจจากข้อ 3 | **ยังไม่ได้แต้ม** สถานะรออนุมัติ |
| 17 | **กดวันที่แอดมินใส่บันทึกไว้ในปฏิทิน** | เด้งหน้าต่างขึ้นมา **อ่านข้อความได้เต็ม** ← แก้รอบนี้ |
| 18 | กลับหน้าแรก รอประมาณ 1 วินาที | **ประกาศเด้ง pop-up ขึ้นมา** · กดปิดแล้ววันนี้ไม่เด้งซ้ำ ← ใหม่รอบนี้ |
| 19 | แลกของรางวัลจากข้อ 4 | แต้มถูกหัก 10 · สต็อกเหลือ 1 |

## หน้าอันดับ — ตรวจเรื่องความลับ (20–21)

| # | ทำ | ต้องได้ |
|:--:|---|---|
| 20 | หน้าอันดับ → กด **ดูทั้งหมด** | เห็นครบทุกคน · ใต้ชื่อขึ้น **ตำแหน่ง • แผนก** · **ไม่มีรหัสพนักงานสักที่** · แถวตัวเองยังมีป้าย YOU |
| 21 | กด `F12` → แท็บ **Network** → รีเฟรช → เปิดคำขอ `public_page_payload` | ในผลลัพธ์ `emp_id` ของคนอื่น**ต้องเป็น `null`** มีแต่ของตัวเองที่เป็นเลขจริง |

> ข้อ 21 คือข้อพิสูจน์ว่ารหัสถูกซ่อนจริงตั้งแต่ฐานข้อมูล ไม่ใช่แค่ไม่แสดงบนหน้าจอ
> ถ้ายังเห็นรหัสคนอื่นเป็นเลข แปลว่า **B6 ยังไม่ได้รัน**

## วนกลับฝั่งแอดมิน (22–23)

| # | ทำ | ต้องได้ |
|:--:|---|---|
| 22 | หน้าภารกิจที่ส่งเข้ามา → กดอนุมัติ | พนักงานได้ 20 แต้ม อันดับขยับ |
| 23 | สมุดแต้ม / กิจกรรมรวม | เห็นครบทุกหมวด: แต้ม เช็คอิน ข่าว ภารกิจ แลกของ เข้าระบบ |

> **ข้อ 2 · 3 · 4 · 12 · 13 คือหัวใจ** ผ่านครบ = Apps Script + Drive +
> Script Properties กลับมาทำงานครบวงจร
>
> **ถ้าได้รูปแตก** กด `F12` → **Console** → ส่งข้อความ error มาให้ผม
> และแท็บ **Network** ดูคำขอไป `script.google.com` ว่าตอบอะไรกลับมา

---

# ช่วง F — สำรองครั้งสุดท้าย แล้วเปิดใช้

## F1 · สำรอง

```
D:\Projectsbconnect_app\push.bat
```

commit message เช่น `go-live: roster 336, privacy fixes, calendar notes, 3-way backup`

รอไอคอน Google Drive มุมขวาล่างขึ้นว่าอัปโหลดเสร็จ **ก่อนปิดเครื่อง**

## F2 · เปิดใช้จริง

1. รีเฟรชแอปหนึ่งรอบ ตรวจว่าเป็นเวอร์ชันใหม่
2. แจ้งพนักงาน: เข้าที่ `https://sbconnection.salary-sbinterlab.workers.dev`
   ใส่ **รหัสพนักงาน** + **รหัสกลาง** แล้วระบบจะให้ตั้งรหัสใหม่ของตัวเอง
3. บอกให้กด **ติดตั้ง / Add to Home Screen** เพื่อใช้แบบแอป
4. เตรียมรับเคส "เข้าไม่ได้" — แอดมินรีเซ็ตรหัสให้ได้จากหน้าแอดมิน

---

# ตารางสรุป: ขั้นไหนย้อนได้ ขั้นไหนไม่ได้

| ขั้น | ย้อนกลับ | วิธีย้อน |
|---|:--:|---|
| A ทั้งหมด | ✅ | ไม่ได้เปลี่ยนอะไร |
| B1 `29_` B2 `30_` B3 `32_` | ✅ | รันซ้ำได้ / อ่านอย่างเดียว |
| B4 `31_` | ⚠️ | รันซ้ำได้ ถ้าจะย้อนจริงต้องกู้จาก Supabase backup (A4) |
| **B5 `26_`** | ❌ | **ย้อนไม่ได้** กู้จาก Supabase backup (A4) เท่านั้น |
| B6 `35_` | ✅ | เป็น `create or replace` รันซ้ำได้ · ถ้าจะย้อนก็รัน `29_` ทับกลับ |
| B7 `18_` B8 `34_` | ✅ | อ่านอย่างเดียว |
| C ทั้งหมด | ✅ | Apps Script เก็บเวอร์ชันเก่า · deployment ย้อนเวอร์ชันได้ |
| D `push.bat` | ⚠️ | `git revert` ได้ แต่ backup ที่เขียนไปแล้วไม่ย้อน |
| E ทั้งหมด | ✅ | เป็นการทดสอบ ลบทิ้งทีหลังได้ |

---

# สิ่งที่ยังไม่พร้อม — อย่าเพิ่งเปิดให้พนักงานใช้

| ระบบ | สถานะจริง |
|---|---|
| **เกมกงล้อ** | มีแค่ไฟล์หน้าตา `src/games/wheel/WheelGame.tsx` · **ยังไม่ถูก import ที่ไหนเลย** · ไม่มีตาราง ไม่มี RPC `spin_wheel` ไม่มีหน้าแอดมิน → พนักงานมองไม่เห็น ปล่อยไว้ได้ |
| **IT Request** | `sql/27_` `28_` เตรียมไว้แต่ยังไม่ได้รัน → ไม่กระทบระบบปัจจุบัน |
| **Quotation** | hold ตามที่คุยกัน |
| **แต้มทศนิยม** | `sql/33_` เป็นสคริปต์ตรวจ ยังไม่ได้แปลง · **ถ้าจะทำ ต้องทำก่อนเขียนระบบเกม** |

---

# เรื่องที่ควรทำต่อ แต่ยังไม่ได้ทำรอบนี้

**บันทึกในปฏิทินมีได้แค่บรรทัดเดียว** ฝั่งแอดมินเก็บข้อความลงช่อง `label`
ช่องเดียวเท่านั้น ยังไม่มีช่องรายละเอียดยาว ๆ แยกต่างหาก

ตอนนี้หน้าผู้ใช้อ่าน `label` มาแสดงได้แล้ว และเผื่อช่อง `detail` / `note` ไว้ด้วย
ถ้าวันไหนเพิ่มคอลัมน์ในฐานข้อมูล หน้าเว็บจะแสดงให้เองทันทีโดยไม่ต้องแก้โค้ดอีก

ถ้าอยากได้ประกาศยาว ๆ มีหัวข้อกับเนื้อหาแยกกัน บอกได้ ผมทำให้ — ต้องเพิ่ม
คอลัมน์ 1 ช่อง + แก้หน้าแอดมิน + แก้ RPC หนึ่งตัว

---

# สิ่งที่ผมยืนยันแทนคุณไม่ได้

1. **เข้า Supabase production ไม่ได้** ตัวเลขทุกตัวในช่วง B ต้องมาจากการรันจริง
2. **เข้า Google Drive และ Apps Script ของคุณไม่ได้** สถานะ Script Properties
   ต้องมาจาก `SB_CHECK_LEGACY` เท่านั้น
3. **เข้า `\\server-sblab\Project_IT` ไม่ได้** ยืนยันไม่ได้จนกว่าจะรัน A2
4. **ไม่มี iPhone ให้ทดสอบ** ข้อ 10–11 คือการแก้ตามสาเหตุที่วิเคราะห์จากภาพหน้าจอ
   (ตัวหนังสือกลับด้าน = เห็นหน้าหลังซ้อนหน้าหน้า = `backface-visibility` ไม่ทำงาน)
   **ต้องให้คุณยืนยันบนเครื่องจริง**
5. **รัน `npm run dev` / `npm run check` จากที่นี่ไม่ได้** (npm ถูกบล็อก)

**สิ่งที่ตรวจไปแล้วจริงรอบนี้**

- `UserDashboard.tsx` ผ่าน esbuild parser ไม่มี syntax error · ตรวจว่าไม่มีตัวแปรค้าง
- `sql/35_PRIVACY_PROFILE_FIXES.sql` **รันจริงบน PostgreSQL 16** พร้อมสร้างสคีมาจำลอง
  แล้วทดสอบ 6 เคส ผ่านทั้งหมด: ซ่อนรหัสคนอื่น (เห็น 1 จาก 3 แถว) · เห็นรหัสตัวเอง ·
  `is_me` ถูกต้องทั้งสองบัญชี · `avatar_url`/`position`/`department` มาครบในหน้าแรก ·
  อันดับหน้าแรกซ่อนรหัสด้วย · session หมดอายุตอบ `SESSION_EXPIRED` ไม่พัง
- ไฟล์ `.bat` ทั้ง 3 ตัว ASCII ล้วน CRLF วงเล็บสมดุล label ครบ ไม่มี `/MIR`

---

# สิ่งที่ผมรอจากคุณ

1. **log ของ `SB_CHECK_LEGACY`** (C4)
2. **ตารางผลของ `34_DRIVE_ASSETS_REPORT.sql`** (B8)
3. **รายการไฟล์ใน Apps Script + จำนวน Script Properties** (C1)
4. **ผลข้อ 10, 13, 17, 18, 21** — ห้าข้อที่แก้รอบนี้ ต้องยืนยันจากเครื่องจริง
